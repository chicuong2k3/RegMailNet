@page "/test"

<div class="max-w-2xl space-y-6">
    <h1 class="text-3xl font-bold tracking-tight text-foreground">Test Page</h1>
    <p class="text-muted-foreground">If you can see this, routing works.</p>
    <BbButton @onclick="@(() => NavigationManager.NavigateTo("/"))">Go Home</BbButton>
</div>

@code {
    [Inject] NavigationManager NavigationManager { get; set; } = default!;
}