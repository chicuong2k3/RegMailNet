# RegMailNet - Coding Conventions

## C# Rules

- Always use `string.Empty` instead of `""`
- Always use `enum` whenever possible, never magic strings
- Never use magic strings — use `const`, `enum`, or `nameof()` instead
